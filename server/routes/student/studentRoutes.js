// // studentRoutes.js


// // Route to get all students assigned to a staff member
// router.get('/staff/:id/students', studentController.getAllStudents);

// // Route to create a new student assigned to a staff member
// router.post('/staff/:id/students', studentController.createStudent);

// // Route to update a student by ID
// router.put('/staff/:staffId/students/:id', studentController.updateStudent);

// // Route to delete a student by ID
// router.delete('/staff/:staffId/students/:id', studentController.deleteStudent);

// module.exports = router;

// const express = require("express");
// const { getAllStudents, createStudent, updateStudent, deleteStudent } = require("../../controller/staff/staffController");
// const router = express.Router();

// router.route("/staff/:id/students").post(createStudent);
// router.route("/staff/:id/students").get(getAllStudents);
// router.route("/staff/:staffId/students/:id").put(updateStudent);
// router.route("/staff/:staffId/students/:id").delete(deleteStudent);



// module.exports = router;

